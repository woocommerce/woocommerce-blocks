const { params } = require( './params' );
const { returns } = require( './returns' );
const { example } = require( './example' );
const { related } = require( './related' );

module.exports = { params, returns, example, related };
